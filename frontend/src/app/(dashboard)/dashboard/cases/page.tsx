/**
 * Section 65B Cases Hub SubPage — Re-export for /dashboard/cases route
 */

import Section65bCasesPage from "../../cases/page";

export default function DashboardCasesSubPage() {
  return <Section65bCasesPage />;
}
